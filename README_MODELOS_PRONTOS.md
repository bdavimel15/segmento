# Update: 28 modelos prontos

Arquivo alterado:
- database/seeders/SegmentoPresetSeeder.php

Depois de copiar para o projeto, rode:

php artisan optimize:clear
php artisan db:seed --class=SegmentoPresetSeeder --force
